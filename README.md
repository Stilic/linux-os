# Nat's Experimental Linux Distribution (NELD)

## How to build

```sh
# Setup the sources
./setup.sh
# Compile everything
./compile.sh
# Build the initramfs
./build_cpio.sh
```

## How to run

```sh
# Run the current built initramfs
./run.sh
```
